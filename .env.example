# Hyperliquid Super Signal - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Hyperliquid Wallet Configuration
# =============================================================================

# Your wallet private key (with 0x prefix)
# NEVER commit your actual private key to version control!
HL_WALLET_PRIVATE_KEY=

# =============================================================================
# OPTIONAL: Environment Settings
# =============================================================================

# Set to 'production' for production logging level (info), otherwise uses 'trace'
NODE_ENV=development

# =============================================================================
# OPTIONAL: Risk Configuration (for $200 account)
# =============================================================================

# High-conviction configuration for $200 account
# Quality-over-quantity: larger risk on rare A+ setups

# Maximum position size in USD (default: 150 for single position focus)
RISK_MAX_POSITION_SIZE=150

# Maximum total exposure across all positions in USD (single position focus)
RISK_MAX_TOTAL_EXPOSURE=150

# Stop loss as percentage, e.g., 0.02 for 2% (default: 0.02)
RISK_STOP_LOSS_PERCENTAGE=0.02

# Maximum drawdown as percentage before halting (20% = $40 for $200 account)
RISK_MAX_DRAWDOWN=0.20

# Percentage of account to risk per trade (5% = $10 for high-conviction setups)
RISK_PERCENTAGE=0.05

# =============================================================================
# OPTIONAL: Strategy Configuration
# =============================================================================

# Entry mode: 'strict' (all 4 stochastics in extreme) or 'relaxed' (Fast+Medium only)
# 'strict' = rare but high winrate quad extreme signals
# Default: strict (quality-over-quantity approach)
STRATEGY_ENTRY_MODE=strict

# =============================================================================
# OPTIONAL: Dry-Run Paper Trading Mode
# =============================================================================

# Enable dry-run mode (uses mainnet data but simulates trades)
# Set to 'true' for paper trading, 'false' or unset for live trading
DRY_RUN=true

# Starting balance for dry-run simulation (USD)
DRY_RUN_BALANCE=200

# =============================================================================
# NOTES
# =============================================================================
# - The bot defaults to TESTNET mode for safety
# - Ensure your wallet has funds on Hyperliquid testnet before running
# - Get testnet funds at: https://app.hyperliquid-testnet.xyz/
